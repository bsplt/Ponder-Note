---
phase: 02-note-identity-&-sidecar-metadata
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src-tauri/src/domain/note.rs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Sidecar JSON writes include created_at matching the note filename"
    - "Existing sidecars using createdAt/updatedAt still load without errors"
  artifacts:
    - path: "app/src-tauri/src/domain/note.rs"
      provides: "NoteSidecar serde naming and aliases for created_at/updated_at"
      contains: "NoteSidecar"
    - path: "app/src-tauri/src/domain/note.rs"
      provides: "Serde coverage for snake_case write and camelCase read"
      contains: "#[cfg(test)]"
  key_links:
    - from: "app/src-tauri/src/domain/note.rs"
      to: "app/src-tauri/src/services/workspace_service.rs"
      via: "write_sidecar serialization"
      pattern: "write_sidecar\(.*NoteSidecar"
    - from: "app/src-tauri/src/services/workspace_service.rs"
      to: "app/src-tauri/src/domain/note.rs"
      via: "parse_sidecar_value deserialization"
      pattern: "serde_json::from_value::<NoteSidecar>"
---

<objective>
Fix sidecar JSON field naming to use snake_case while keeping backwards-compatible reads.

Purpose: Close NOTE-03 gap by writing created_at/updated_at and accepting legacy createdAt.
Output: Updated NoteSidecar serde configuration and tests covering serialization/deserialization.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-note-identity-&-sidecar-metadata/02-note-identity-&-sidecar-metadata-VERIFICATION.md
@app/src-tauri/src/domain/note.rs
@app/src-tauri/src/services/workspace_service.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch NoteSidecar serialization to snake_case</name>
  <files>app/src-tauri/src/domain/note.rs</files>
  <action>
    Update NoteSidecar serde attributes so JSON writes use created_at/updated_at (snake_case).
    - Remove camelCase rename_all from NoteSidecar.
    - Add explicit snake_case naming (rename_all = "snake_case" or per-field renames) and keep title/tags unchanged.
    - Add serde alias for createdAt/updatedAt to preserve legacy reads.
    - Keep NoteSummary serialization as camelCase for frontend API compatibility.
  </action>
  <verify>nix --extra-experimental-features "nix-command flakes" develop -c cargo check</verify>
  <done>NoteSidecar serializes created_at/updated_at and still deserializes legacy createdAt/updatedAt.</done>
</task>

<task type="auto">
  <name>Task 2: Add serde compatibility tests for sidecar JSON</name>
  <files>app/src-tauri/src/domain/note.rs</files>
  <action>
    Add a #[cfg(test)] module in note.rs with serde_json tests that:
    - Serialize NoteSidecar and assert the JSON contains "created_at" and does not contain "createdAt".
    - Deserialize from JSON containing "createdAt"/"updatedAt" and assert fields populate correctly.
    Keep tests focused on NoteSidecar; avoid changing NoteSummary behavior.
  </action>
  <verify>nix --extra-experimental-features "nix-command flakes" develop -c cargo test</verify>
  <done>Tests pass and enforce snake_case output plus camelCase alias input.</done>
</task>

</tasks>

<verification>
- `nix --extra-experimental-features "nix-command flakes" develop -c cargo test` (app/src-tauri)
</verification>

<success_criteria>
- Sidecar JSON written by the app uses `created_at`/`updated_at` fields.
- Existing sidecars with `createdAt`/`updatedAt` continue to load without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-note-identity-&-sidecar-metadata/02-04-SUMMARY.md`
</output>
