---
phase: 03-overview-editor-loop
plan: 04
type: execute
wave: 4
depends_on:
  - "03-overview-editor-loop-03"
files_modified:
  - app/src/screens/Editor.tsx
  - app/src/api/notes.ts
  - app/src-tauri/src/commands/notes.rs
  - app/src-tauri/src/services/workspace_service.rs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Autosave completes successfully and updates the last saved timestamp after edits."
    - "Save failures surface the underlying error message for troubleshooting."
    - "Exit saves (with checklist rewrite) complete without showing the exit warning."
  artifacts:
    - path: "app/src/screens/Editor.tsx"
      provides: "Autosave state, error banner, lastSavedAt updates, and exit-save flow"
    - path: "app/src/api/notes.ts"
      provides: "note_save invoke wrapper with error propagation"
    - path: "app/src-tauri/src/commands/notes.rs"
      provides: "note_save command returning detailed errors"
    - path: "app/src-tauri/src/services/workspace_service.rs"
      provides: "note_save implementation and atomic write"
  key_links:
    - from: "app/src/screens/Editor.tsx"
      to: "app/src/api/notes.ts"
      via: "noteSave in performSave"
      pattern: "noteSave"
    - from: "app/src/api/notes.ts"
      to: "note_save"
      via: "invoke"
      pattern: "invoke<CommandResult<null>>\\('note_save'"
    - from: "app/src-tauri/src/commands/notes.rs"
      to: "app/src-tauri/src/services/workspace_service.rs"
      via: "svc.note_save"
      pattern: "note_save\\(&stem"
---

<objective>
Resolve autosave and exit-save failures by surfacing the real error and fixing the underlying note_save path so successful saves update state and do not trigger warnings.

Purpose: Close UAT gaps blocking reliable autosave and exit behavior.
Output: Updated note save error handling in the editor and a corrected backend save path.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-overview-editor-loop/03-overview-editor-loop-UAT.md
@.planning/phases/03-overview-editor-loop/03-03-SUMMARY.md
@app/src/screens/Editor.tsx
@app/src/api/notes.ts
@app/src-tauri/src/commands/notes.rs
@app/src-tauri/src/services/workspace_service.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose note_save failure and fix backend path</name>
  <files>app/src-tauri/src/commands/notes.rs, app/src-tauri/src/services/workspace_service.rs</files>
  <action>
    Reproduce the failing save path using the editor flow or a targeted note_save call and capture the error details.
    Update the note_save command/service to resolve the root cause (e.g., incorrect stem handling, workspace path resolution, or atomic write failures) so note_save returns ok for valid notes.
    Ensure any remaining errors include actionable context in the message (stem, workspace path) so frontend can surface them.
    Do NOT change the atomic write strategy or rewrite rules; only fix the failure cause and error messaging.
  </action>
  <verify>nix develop --extra-experimental-features "nix-command flakes" --command cargo check</verify>
  <done>note_save succeeds for existing and new notes and returns CommandResult ok for valid saves.</done>
</task>

<task type="auto">
  <name>Task 2: Surface save errors and clear banner on success</name>
  <files>app/src/screens/Editor.tsx, app/src/api/notes.ts</files>
  <action>
    Expand the save error handling to include the error code/message from NoteApiError so the banner shows the actual failure cause.
    On successful saves, always clear the banner and update lastSavedAt (including the exit save path) so the header timestamp changes.
    Ensure saveError state does not persist after a successful retry or exit save.
  </action>
  <verify>nix develop --extra-experimental-features "nix-command flakes" --command npm --prefix app run build</verify>
  <done>Autosave failures display concrete error info and successful saves clear the banner and update the last saved timestamp.</done>
</task>

</tasks>

<verification>
- Run the app and confirm autosave completes without the error banner and updates “Last saved”.
- Exit the editor after edits; confirm no exit warning appears and the file content persists with checklist rewrites applied.
</verification>

<success_criteria>
- Autosave completes without triggering the failure banner for a normal edit.
- Exit save completes without showing the confirmation warning.
- The banner includes the underlying error message when a save does fail.
</success_criteria>

<output>
After completion, create `.planning/phases/03-overview-editor-loop/03-04-SUMMARY.md`
</output>
