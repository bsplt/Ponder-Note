---
phase: 01-workspaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/src/App.tsx
  - app/src/screens/Overview.tsx
  - app/src/screens/Workspaces.tsx
  - app/src/api/tauri.ts
  - app/src/styles.css
  - app/src-tauri/Cargo.toml
  - app/src-tauri/src/main.rs
  - app/src-tauri/capabilities/default.json
autonomous: true

must_haves:
  truths:
    - "A Tauri dev build launches a window and renders the Overview and Workspaces screens."
    - "The app can open a native folder picker for selecting a workspace folder."
  artifacts:
    - path: app/package.json
      provides: "JS workspace and dev scripts"
    - path: app/src-tauri/src/main.rs
      provides: "Tauri v2 app entrypoint"
    - path: app/src/App.tsx
      provides: "Top-level screen shell"
  key_links:
    - from: app/src/App.tsx
      to: app/src/screens/Overview.tsx
      via: "screen routing (local state)"
      pattern: "Overview"
    - from: app/src/App.tsx
      to: app/src/screens/Workspaces.tsx
      via: "screen routing (local state)"
      pattern: "Workspaces"
    - from: app/src-tauri/src/main.rs
      to: app/src-tauri/capabilities/default.json
      via: "Tauri v2 capability allowlist"
      pattern: "capabil"
---

<objective>
Bootstrap a runnable Tauri v2 app scaffold (in `app/`) with a minimal UI shell (Overview + Workspaces screens) and dialog plugin wiring.

Purpose: Provide a testable foundation so Phase 01 workspace behaviors can be implemented and verified.
Output: Tauri app scaffold in `app/` that can run via `pnpm tauri dev` inside `nix develop`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-workspaces/01-CONTEXT.md
@.planning/phases/01-workspaces/01-RESEARCH.md
@flake.nix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri v2 app under app/</name>
  <files>
app/package.json
app/src-tauri/src/main.rs
  </files>
  <action>
- Enter the Nix dev shell for all commands: `nix develop`.
- Scaffold a Tauri v2 app into `app/` (do NOT scaffold into repo root since it is not empty). Use a Vite + React + TypeScript template.
- Ensure the scaffold includes:
  - `app/src-tauri/` (Rust side)
  - `app/src/` (web UI)
  - A working `pnpm tauri dev` script (or equivalent) in `app/package.json`.
- Keep any generated defaults that are required for Tauri v2 capabilities to function.

Important: this phase will rely on Tauri IPC commands; avoid templates that disable IPC or omit capability config.
  </action>
  <verify>
- `nix develop -c pnpm -C app install`
- `nix develop -c pnpm -C app tauri dev` (window opens)
  </verify>
  <done>
- `app/` contains a runnable Tauri app scaffold; `pnpm -C app tauri dev` launches successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add dialog plugin (folder picker + confirm) end-to-end</name>
  <files>
app/package.json
app/src-tauri/Cargo.toml
app/src-tauri/src/main.rs
app/src-tauri/capabilities/default.json
app/src/api/tauri.ts
  </files>
  <action>
- Add Tauri dialog plugin on both sides:
  - Rust crate `tauri-plugin-dialog` (2.6.x).
  - JS package `@tauri-apps/plugin-dialog`.
- Register the plugin in `app/src-tauri/src/main.rs`.
- Ensure the generated/required capability permissions for dialog are present (prefer using the official add flow if available in this scaffold; do not guess permission identifiers).
- Create `app/src/api/tauri.ts` with small wrappers used later:
  - `pickDirectory(): Promise<string | null>` using `open({ directory: true, multiple: false })`.
  - `confirmReplace(message: string): Promise<boolean>` using `confirm()`.
  </action>
  <verify>
- In `pnpm -C app tauri dev`, temporarily add a button in `app/src/App.tsx` that opens the directory picker and logs the selected path; confirm it works on macOS.
- Remove the temporary button once verified.
  </verify>
  <done>
- The renderer can open a native directory picker and a confirm dialog.
- Capability/permission wiring allows the plugin to run without IPC permission errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create minimal UI shell: Overview + Workspaces screens</name>
  <files>
app/src/App.tsx
app/src/screens/Overview.tsx
app/src/screens/Workspaces.tsx
app/src/styles.css
  </files>
  <action>
- Implement a tiny in-app routing mechanism in `app/src/App.tsx` (no router dependency yet):
  - `screen: 'overview' | 'workspaces'` state.
  - Render `Overview` and `Workspaces` components.
  - Provide a visible header showing the current screen.
- Create placeholders:
  - `Overview.tsx`: show “No workspace selected yet” placeholder.
  - `Workspaces.tsx`: render 9 slots (1-9) with placeholder “Unassigned”.
- Add a minimal `app/src/styles.css` so the UI is readable (basic layout, monospace path styling, hover/focus states).

Note: Do not implement workspace logic in this plan; keep it purely structural.
  </action>
  <verify>
- `nix develop -c pnpm -C app tauri dev` renders Overview and can navigate to Workspaces via a temporary button.
  </verify>
  <done>
- Both screens exist and render; navigation between them works without external routing libraries.
  </done>
</task>

</tasks>

<verification>
- `pnpm -C app tauri dev` launches and renders UI.
- Dialog plugin is callable from the renderer without capability errors.
</verification>

<success_criteria>
- A runnable Tauri v2 scaffold exists in `app/` and provides the UI surface + dialog primitives required to implement workspace selection in later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspaces/01-01-SUMMARY.md`
</output>
