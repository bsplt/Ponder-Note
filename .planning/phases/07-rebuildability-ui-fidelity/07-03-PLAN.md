---
phase: 07-rebuildability-ui-fidelity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/styles.css
  - app/src/screens/Editor.tsx
  - app/src/screens/Overview.tsx
  - app/src/screens/TodoList.tsx
  - app/src/screens/Workspaces.tsx
  - app/src/components/SearchBar.tsx
  - app/src/components/PillInput.tsx
  - app/src/components/TagAutocomplete.tsx
  - app/src/components/Toast.tsx
  - FIGMA-DEVIATIONS.md
autonomous: true
must_haves:
  truths:
    - "UI styles are centralized in CSS tokens and can be adjusted without editing components"
    - "Editor content column width and font size scale within defined min/max bounds"
    - "UI matches Figma layouts/components; deviations are documented"
  artifacts:
    - path: "app/src/styles.css"
      provides: "Global design tokens + component styling"
    - path: "app/src/screens/Editor.tsx"
      provides: "Editor layout hooks for responsive column"
    - path: "FIGMA-DEVIATIONS.md"
      provides: "Documented intentional deviations"
  key_links:
    - from: "app/src/styles.css"
      to: "app/src/screens/Editor.tsx"
      via: "responsive editor column classes"
      pattern: "editor"
    - from: "app/src/styles.css"
      to: "app/src/components/SearchBar.tsx"
      via: "shared tokenized input styles"
      pattern: "searchBar"
---

<objective>
Centralize styling tokens, make the editor column responsive, and align UI to Figma with documented deviations.

Purpose: Deliver UI-01/UI-02/UI-03 with CSS-first theming and readability.
Output: Tokenized styles, responsive editor layout, and Figma deviation notes.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-rebuildability-ui-fidelity/07-CONTEXT.md
@app/src/styles.css
@app/src/screens/Editor.tsx
@app/src/screens/Overview.tsx
@app/src/screens/TodoList.tsx
@app/src/screens/Workspaces.tsx
@app/src/components/SearchBar.tsx
@app/src/components/PillInput.tsx
@app/src/components/TagAutocomplete.tsx
@app/src/components/Toast.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize global UI styles</name>
  <files>app/src/styles.css</files>
  <action>
    Expand `:root` to include tokens for colors, typography, spacing, radii, and shadows (single theme). Replace literal values throughout the stylesheet with tokens. Keep token names consistent and scoped (e.g., --color-*, --space-*, --radius-*, --font-*, --shadow-*). Ensure all app surfaces (Overview, Editor, Todo List, Workspaces, Toasts) use tokenized styles and no per-component hardcoded colors remain.
  </action>
  <verify>
    Review `app/src/styles.css` to confirm most literals are replaced by tokens and UI still renders correctly in dev.
  </verify>
  <done>
    The UI styling is centralized in CSS variables and easy to theme without editing components.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement responsive editor column readability</name>
  <files>
    app/src/styles.css
    app/src/screens/Editor.tsx
  </files>
  <action>
    Add a centered editor content column with `max-width` in `ch` (target 60-72ch) and fluid font sizing via `clamp()` with explicit min/max bounds. Reduce padding on narrow windows and keep the column centered. Update Editor markup to wrap the textarea in the new layout class and ensure the textarea inherits the responsive font size and line-height.
  </action>
  <verify>
    Resize the app window from narrow to wide and confirm the editor column width and base font size scale smoothly within defined bounds.
  </verify>
  <done>
    Editor text remains readable at all window widths with a centered column and fluid type scale.
  </done>
</task>

<task type="auto">
  <name>Task 3: Align UI to Figma and document deviations</name>
  <files>
    app/src/styles.css
    app/src/screens/Overview.tsx
    app/src/screens/TodoList.tsx
    app/src/screens/Workspaces.tsx
    app/src/components/SearchBar.tsx
    app/src/components/PillInput.tsx
    app/src/components/TagAutocomplete.tsx
    app/src/components/Toast.tsx
    FIGMA-DEVIATIONS.md
  </files>
  <action>
    Compare Overview, Editor, Todo List, and Workspaces screens to Figma and adjust spacing, typography, colors, and component states (buttons, inputs, list rows) to closely match. Only change markup if required to match Figma. Record any intentional deviations (with reason) in `FIGMA-DEVIATIONS.md` using a short bullet list per screen.
  </action>
  <verify>
    Visual diff against Figma shows close match; deviations are documented in `FIGMA-DEVIATIONS.md`.
  </verify>
  <done>
    UI matches Figma with documented exceptions and consistent tokens across screens.
  </done>
</task>

</tasks>

<verification>
- CSS variables are used consistently across app surfaces.
- Editor column remains centered and readable at small and large window sizes.
- `FIGMA-DEVIATIONS.md` lists any deliberate mismatches.
</verification>

<success_criteria>
- UI-01, UI-02, and UI-03 are satisfied with centralized styling and documented Figma deviations.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rebuildability-ui-fidelity/07-03-SUMMARY.md`
</output>
