---
phase: 07-rebuildability-ui-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src-tauri/src/domain/rebuild.rs
  - app/src-tauri/src/domain/mod.rs
  - app/src-tauri/src/services/workspace_service.rs
  - app/src-tauri/src/commands/workspace.rs
  - app/src-tauri/src/lib.rs
autonomous: true
must_haves:
  truths:
    - "When .ponder is missing, derived state is rebuilt without modifying Markdown note contents"
    - "A rebuild log is written for each rebuild and can be retrieved by command"
    - "Rebuild runs on app launch and when the active workspace changes"
  artifacts:
    - path: "app/src-tauri/src/domain/rebuild.rs"
      provides: "Rebuild log and summary structs"
    - path: "app/src-tauri/src/services/workspace_service.rs"
      provides: "Idempotent rebuild routine + log writer"
    - path: "app/src-tauri/src/commands/workspace.rs"
      provides: "workspace_rebuild + workspace_get_rebuild_log commands"
    - path: "app/src-tauri/src/lib.rs"
      provides: "Command registration for rebuild endpoints"
  key_links:
    - from: "app/src-tauri/src/commands/workspace.rs"
      to: "app/src-tauri/src/services/workspace_service.rs"
      via: "workspace_rebuild + workspace_get_state triggers"
      pattern: "rebuild"
    - from: "app/src-tauri/src/services/workspace_service.rs"
      to: ".ponder/rebuild-log.json"
      via: "atomic log write"
      pattern: "rebuild-log"
---

<objective>
Add an idempotent rebuild routine that recreates derived workspace state and logs rebuild outcomes.

Purpose: Satisfy SAFE-03 by rebuilding .ponder data safely and silently.
Output: Rust rebuild routine, rebuild log format, and commands to run/read logs.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/devlog/planning/PROJECT.md
@docs/devlog/planning/ROADMAP.md
@docs/devlog/planning/STATE.md
@docs/devlog/planning/phases/07-rebuildability-ui-fidelity/07-CONTEXT.md
@docs/devlog/planning/phases/07-rebuildability-ui-fidelity/07-RESEARCH.md
@app/src-tauri/src/services/workspace_service.rs
@app/src-tauri/src/commands/workspace.rs
@app/src-tauri/src/domain/note.rs
@app/src-tauri/src/domain/workspace.rs
@app/src-tauri/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define rebuild log schema and IO helpers</name>
  <files>
    app/src-tauri/src/domain/rebuild.rs
    app/src-tauri/src/domain/mod.rs
    app/src-tauri/src/services/workspace_service.rs
  </files>
  <action>
    Create a rebuild log schema that captures started/finished timestamps, workspace path, counts (notes scanned, sidecars created, sidecars repaired), and a list of errors. Serialize with camelCase for UI. Add log read/write helpers in workspace_service using NamedTempFile for atomic log writes to `<workspace>/.ponder/rebuild-log.json`. Return Option<RebuildLog> when the log is missing or unreadable (do not error on missing file). Keep all Markdown note reads as read-only and never write to .md files in rebuild.
  </action>
  <verify>
    Inspect compiled code to confirm rebuild log types are in domain module and log writes use NamedTempFile.
  </verify>
  <done>
    Rebuild log structs exist, are serializable, and log IO helpers write/read `.ponder/rebuild-log.json` without touching Markdown.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement rebuild routine and wire commands</name>
  <files>
    app/src-tauri/src/services/workspace_service.rs
    app/src-tauri/src/commands/workspace.rs
    app/src-tauri/src/lib.rs
  </files>
  <action>
    Add an idempotent `rebuild_active_workspace` routine in WorkspaceService that:
    - Ensures `<workspace>/.ponder/`, `<workspace>/.ponder/meta/`, and `<workspace>/.ponder/index/` exist.
    - Rebuilds sidecars for each root `*.md` by reading first line, deriving title, and using existing sidecar logic to preserve extra fields and fix missing/invalid sidecars.
    - Writes a minimal index marker under `.ponder/index/version.json` (schema_version + created_at) without altering note contents.
    - Collects counts and errors into a RebuildLog and writes it via the log helper.

    Expose `workspace_rebuild` (returns RebuildLog) and `workspace_get_rebuild_log` (returns Option<RebuildLog>) commands. Trigger rebuild silently on app launch and workspace changes by calling `rebuild_active_workspace` after successful `workspace_get_state`, `workspace_assign_slot`, and `workspace_switch_slot` when active status is ok. Keep errors inside the log; only return command errors for lock poisoning or invalid workspace state.
  </action>
  <verify>
    Check that new commands are registered in `app/src-tauri/src/lib.rs` and invoked in `app/src-tauri/src/commands/workspace.rs`.
  </verify>
  <done>
    Rebuild runs on boot/slot change, recreates `.ponder` derived state, and writes a structured log without modifying Markdown files.
  </done>
</task>

</tasks>

<verification>
- Rebuild logic only writes to `.ponder/*` and never updates `.md` files.
- `.ponder/rebuild-log.json` is created after rebuild and is readable via command.
</verification>

<success_criteria>
- SAFE-03 backend behavior is implemented and rebuild can run at launch or workspace switch.
</success_criteria>

<output>
After completion, create `docs/devlog/planning/phases/07-rebuildability-ui-fidelity/07-01-SUMMARY.md`
</output>
