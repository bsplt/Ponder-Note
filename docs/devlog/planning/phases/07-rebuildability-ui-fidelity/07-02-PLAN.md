---
phase: 07-rebuildability-ui-fidelity
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - app/src/api/workspace.ts
  - app/src/components/RebuildLogModal.tsx
  - app/src/App.tsx
  - app/src/styles.css
autonomous: true
must_haves:
  truths:
    - "User can open a rebuild log view that shows the last rebuild summary"
    - "User can open the rebuild log file in the OS"
  artifacts:
    - path: "app/src/api/workspace.ts"
      provides: "Typed rebuild log API wrappers"
    - path: "app/src/components/RebuildLogModal.tsx"
      provides: "Modal UI for rebuild log summary"
    - path: "app/src/App.tsx"
      provides: "View rebuild log entry point"
  key_links:
    - from: "app/src/App.tsx"
      to: "app/src/components/RebuildLogModal.tsx"
      via: "header action -> modal open"
      pattern: "RebuildLog"
    - from: "app/src/components/RebuildLogModal.tsx"
      to: "app/src/api/workspace.ts"
      via: "workspace_get_rebuild_log invoke + workspace_get_state path derivation"
      pattern: "workspaceGetRebuildLog"
    - from: "app/src/components/RebuildLogModal.tsx"
      to: "app/src/api/workspace.ts"
      via: "workspace_get_state invoke for active workspace path"
      pattern: "workspaceGetState"
---

<objective>
Expose rebuild log data in the UI and provide a "View rebuild log" action with a summary.

Purpose: Make rebuild status discoverable without interrupting the workflow.
Output: UI modal + API bindings for rebuild log retrieval.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/devlog/planning/PROJECT.md
@docs/devlog/planning/ROADMAP.md
@docs/devlog/planning/STATE.md
@docs/devlog/planning/phases/07-rebuildability-ui-fidelity/07-CONTEXT.md
@app/src/App.tsx
@app/src/api/workspace.ts
@app/src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rebuild log API types and wrappers</name>
  <files>app/src/api/workspace.ts</files>
  <action>
    Define `RebuildLog` and `RebuildLogError` types matching the Rust schema (camelCase fields) and add `workspaceGetRebuildLog()` that invokes `workspace_get_rebuild_log`. Return `RebuildLog | null` for missing logs. Avoid adding new dependencies.
  </action>
  <verify>
    TypeScript typecheck passes and API wrapper compiles with existing invoke patterns.
  </verify>
  <done>
    UI can call a typed `workspaceGetRebuildLog()` to fetch the last rebuild summary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build rebuild log modal and header action</name>
  <files>
    app/src/components/RebuildLogModal.tsx
    app/src/App.tsx
    app/src/styles.css
  </files>
  <action>
    Create a modal component that loads the rebuild log on open, renders summary fields (time range, counts, errors), and includes an "Open log file" button that calls `open()` from `@tauri-apps/plugin-opener`. Derive the log path in the modal by calling `workspaceGetState()` to get the active workspace path and appending `/.ponder/rebuild-log.json`; only enable the open button when the active path exists. Add a "View rebuild log" action in the app header (non-editor screens) that opens the modal. Style the modal in `app/src/styles.css` using existing tokens/classes; include overlay, card, and close button states.
  </action>
  <verify>
    Run the app and open the modal from the header; confirm the modal loads log data and the open-file button uses the derived workspace path to trigger the OS opener.
  </verify>
  <done>
    The rebuild log is accessible from the UI with a readable summary and an OS open-file action.
  </done>
</task>

</tasks>

<verification>
- "View rebuild log" button is visible on non-editor screens and opens a modal.
- Modal shows a no-log empty state when no log exists.
</verification>

<success_criteria>
- UI exposes last rebuild summary and optional file open action without disrupting navigation.
</success_criteria>

<output>
After completion, create `docs/devlog/planning/phases/07-rebuildability-ui-fidelity/07-02-SUMMARY.md`
</output>
