---
phase: 07-rebuildability-ui-fidelity
plan: 04
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
files_modified: []
autonomous: false
must_haves:
  truths:
    - "User confirms rebuild works after deleting .ponder and notes remain unchanged"
    - "User confirms UI matches Figma and responsive editor behaves correctly"
  artifacts:
    - path: "docs/design/FIGMA-DEVIATIONS.md"
      provides: "Documented intentional UI differences"
  key_links:
    - from: "View rebuild log action"
      to: "Rebuild log modal"
      via: "UI verification"
      pattern: "rebuild log"
---

<objective>
Validate rebuildability and UI fidelity with human verification.

Purpose: Confirm SAFE-03 + UI-01/02/03 behavior in real usage.
Output: Human verification of rebuild and Figma alignment.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/devlog/planning/PROJECT.md
@docs/devlog/planning/ROADMAP.md
@docs/devlog/planning/STATE.md
@docs/devlog/planning/phases/07-rebuildability-ui-fidelity/07-CONTEXT.md
@docs/design/FIGMA-DEVIATIONS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Rebuild routine + rebuild log UI + tokenized, responsive styles</what-built>
  <how-to-verify>
    1) Launch the app, open a workspace with notes, then quit.
    2) Delete `<workspace>/.ponder/` entirely.
    3) Relaunch the app and confirm: notes list loads, todos still appear, and note contents are unchanged.
    4) Click "View rebuild log" and confirm the summary looks correct; click "Open log file" to open the JSON log.
    5) Resize the window narrow and wide and verify the editor column stays centered with readable line length and fluid font size.
    6) Compare Overview/Editor/Todo List/Workspaces to Figma; confirm alignment or list deviations in `docs/design/FIGMA-DEVIATIONS.md`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Human verification confirms SAFE-03 and UI-01/UI-02/UI-03 behavior.
</verification>

<success_criteria>
- User approves rebuild behavior and UI fidelity (or provides issues to fix).
</success_criteria>

<output>
After completion, create `docs/devlog/planning/phases/07-rebuildability-ui-fidelity/07-04-SUMMARY.md`
</output>
