---
phase: 02-note-identity-&-sidecar-metadata
plan: 03
type: execute
wave: 3
depends_on:
  - 02-note-identity-&-sidecar-metadata-02
files_modified:
  - app/src/api/workspace.ts
  - app/src/stores/workspaceStore.ts
  - app/src/screens/Overview.tsx
  - app/src/styles.css
autonomous: false

must_haves:
  truths:
    - "Overview renders note titles derived from first-line content"
    - "Overview shows de-DE timestamps with time for today and date-only for older notes"
    - "New Note action creates a note and refreshes the list"
  artifacts:
    - path: "app/src/api/workspace.ts"
      provides: "NoteSummary type + list/create commands"
      contains: "workspace_list_notes"
    - path: "app/src/stores/workspaceStore.ts"
      provides: "notes state and createNote action"
      contains: "createNote"
    - path: "app/src/screens/Overview.tsx"
      provides: "note rows with title + timestamp + New Note button"
      contains: "New Note"
  key_links:
    - from: "app/src/screens/Overview.tsx"
      to: "app/src/stores/workspaceStore.ts"
      via: "workspaceActions.createNote"
      pattern: "createNote"
    - from: "app/src/stores/workspaceStore.ts"
      to: "app/src/api/workspace.ts"
      via: "workspaceListNotes"
      pattern: "workspaceListNotes"
---

<objective>
Update the renderer to display note titles and timestamps from metadata and add a New Note action.

Purpose: Surface stable note identity in the UI and allow creation of timestamp-named notes.
Output: Frontend wiring for note summaries, timestamp formatting, and a New Note button.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/devlog/planning/PROJECT.md
@docs/devlog/planning/ROADMAP.md
@docs/devlog/planning/STATE.md

@app/src/api/workspace.ts
@app/src/stores/workspaceStore.ts
@app/src/screens/Overview.tsx
@app/src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire note list + create APIs into the workspace store</name>
  <files>app/src/api/workspace.ts, app/src/stores/workspaceStore.ts</files>
  <action>
In `app/src/api/workspace.ts`:
- Add a `NoteSummary` type matching backend shape (`stem`, `title`, `createdAt`, `updatedAt`, `tags`, optionally `filename`).
- Add `workspaceListNotes()` and `workspaceCreateNote()` wrappers invoking new Tauri commands and returning typed results.
- Keep `unwrap` error handling consistent with existing API.

In `workspaceStore.ts`:
- Replace `rootNotes: string[]` with `notes: NoteSummary[]` in state.
- Update `refreshNotes` to call `workspaceListNotes` and store `notes`.
- Add `createNote` action that calls `workspaceCreateNote`, then refreshes notes (or prepends returned note) and handles errors with existing patterns.
  </action>
  <verify>nix develop -c pnpm -C app build</verify>
  <done>Store exposes notes metadata and can create a new note via backend</done>
</task>

<task type="auto">
  <name>Task 2: Render title + timestamp and add New Note button</name>
  <files>app/src/screens/Overview.tsx, app/src/styles.css</files>
  <action>
Update `Overview.tsx` to:
- Use `notes` from the store and render each row with title and formatted timestamp.
- Add a `New Note` button in the header area that calls `workspaceActions.createNote()` and shows a toast on success/failure.
- Implement a small formatter (in-file or helper) that uses `Intl.DateTimeFormat('de-DE', ...)`:
  - If note is from today (local date), show time `HH:mm`.
  - Otherwise show date `dd.MM.yyyy`.
Ensure empty lists still show the existing empty-state message.

Update `styles.css` to add layout for the new note row structure (e.g., title + timestamp alignment) and for the New Note button placement.
Avoid changing the existing color palette or panel layout.
  </action>
  <verify>nix develop -c pnpm -C app build</verify>
  <done>Overview displays title + timestamp and a functional New Note button</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Note overview rows with title/timestamp display and New Note action</what-built>
  <how-to-verify>
    1. Run the app and select a valid workspace.
    2. Click the New Note button.
    3. Confirm a new `<unix_ms>.md` file appears in the workspace root and a `.ponder/meta/<stem>.json` sidecar is created.
    4. Verify the note row shows the derived title and a German-formatted timestamp (time for today, date for older notes).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
`nix develop -c pnpm -C app build` passes in `app/` and the UI renders note titles/timestamps.
</verification>

<success_criteria>
The overview list shows correct note titles and timestamps, and users can create timestamp-named notes with sidecars from the UI.
</success_criteria>

<output>
After completion, create `docs/devlog/planning/phases/02-note-identity-&-sidecar-metadata/02-03-SUMMARY.md`
</output>
