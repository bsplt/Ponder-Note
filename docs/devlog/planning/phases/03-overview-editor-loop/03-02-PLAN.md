---
phase: 03-overview-editor-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 03-overview-editor-loop-01
files_modified:
  - app/src-tauri/Cargo.toml
  - app/src-tauri/src/commands/mod.rs
  - app/src-tauri/src/commands/notes.rs
  - app/src-tauri/src/lib.rs
  - app/src-tauri/src/services/workspace_service.rs
  - app/src/api/notes.ts
autonomous: true
must_haves:
  truths:
    - "Note content can be read and saved via Tauri commands using atomic writes."
    - "Exit saves can apply the `o ` rewrite before writing to disk."
    - "Discarding an empty new note removes its .md file and sidecar JSON."
  artifacts:
    - path: "app/src-tauri/src/commands/notes.rs"
      provides: "note_read, note_save, note_discard commands"
      exports: ["note_read", "note_save", "note_discard"]
    - path: "app/src-tauri/src/services/workspace_service.rs"
      provides: "note read/save/discard + atomic write helper"
      min_lines: 40
    - path: "app/src-tauri/Cargo.toml"
      provides: "tempfile dependency for atomic writes"
      contains: "tempfile"
    - path: "app/src/api/notes.ts"
      provides: "frontend invoke wrappers for note IO"
      min_lines: 30
  key_links:
    - from: "app/src-tauri/src/commands/notes.rs"
      to: "app/src-tauri/src/services/workspace_service.rs"
      via: "WorkspaceService note_* methods"
      pattern: "note_(read|save|discard)"
    - from: "app/src-tauri/src/services/workspace_service.rs"
      to: "app/src-tauri/src/domain/note_rewrite.rs"
      via: "rewrite on exit"
      pattern: "rewrite_exit_checklists"
    - from: "app/src-tauri/src/services/workspace_service.rs"
      to: "tempfile::NamedTempFile"
      via: "atomic write"
      pattern: "NamedTempFile::new_in"
---

<objective>
Expose safe note read/save/discard commands backed by atomic writes and the exit rewrite helper.

Purpose: Enables the editor to load/save notes reliably without risking partial files.
Output: Tauri commands + frontend API wrapper for note IO.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/devlog/planning/PROJECT.md
@docs/devlog/planning/ROADMAP.md
@docs/devlog/planning/STATE.md
@docs/devlog/planning/phases/03-overview-editor-loop/03-CONTEXT.md
@docs/devlog/planning/phases/03-overview-editor-loop/03-RESEARCH.md
@app/src-tauri/src/services/workspace_service.rs
@app/src-tauri/src/commands/workspace.rs
@app/src/api/workspace.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add atomic note IO methods and Tauri commands</name>
  <files>
app/src-tauri/Cargo.toml
app/src-tauri/src/commands/mod.rs
app/src-tauri/src/commands/notes.rs
app/src-tauri/src/lib.rs
app/src-tauri/src/services/workspace_service.rs
  </files>
  <action>
Add the `tempfile` dependency and implement atomic write helpers using `NamedTempFile::new_in`, `write_all`, `sync_all`, and `persist`.
Extend `WorkspaceService` with methods to:
- Resolve note paths from stems in the active workspace
- Read note content as UTF-8 string
- Save note content atomically, optionally applying `rewrite_exit_checklists` when `rewrite_on_exit=true`
- Discard a note by removing `<workspace>/<stem>.md` and `<workspace>/.ponder/meta/<stem>.json` (ignore not-found)
Create `commands/notes.rs` with `note_read`, `note_save`, and `note_discard` Tauri commands that call the new methods and return `CommandResult`.
Register the commands in `commands/mod.rs` and `lib.rs` invoke handler.
Avoid direct `std::fs::write` for saves (SAFE-01).
  </action>
  <verify>nix develop -c cargo check -p app</verify>
  <done>
Note IO commands compile and all saves use atomic temp-file replacement with optional exit rewrite.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add frontend notes API wrapper</name>
  <files>app/src/api/notes.ts</files>
  <action>
Create `noteRead(stem)`, `noteSave({ stem, body, rewriteOnExit })`, and `noteDiscard(stem)` invoke wrappers that mirror `CommandResult` handling.
Provide a `NoteApiError` similar to `WorkspaceApiError` for consistent error handling.
Keep the API focused on stems (no absolute paths) to avoid leaking filesystem details into the UI.
  </action>
  <verify>nix develop -c npm run build</verify>
  <done>
Notes API calls resolve via invoke and throw typed errors on command failures.
  </done>
</task>

</tasks>

<verification>
Cargo check and frontend build complete without errors.
</verification>

<success_criteria>
Notes can be read, saved atomically (with optional rewrite), and discarded via Tauri commands exposed to the frontend.
</success_criteria>

<output>
After completion, create `docs/devlog/planning/phases/03-overview-editor-loop/03-02-SUMMARY.md`
</output>
