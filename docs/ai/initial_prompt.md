I want to build an app called Ponder. It is a note-taking app that runs on macOS. The main idea is that I don't have to leave the software to go through my different notes. The notes. are basically meeting protocols. The app consists of three screens. The first is an overview in which I have a list of my notes and I can search the notes deeply with the search field and I can filter the notes by tags that are I give my to notes. I can use mouse navigation or keyboard navigation to open these nodes that are either unfiltered or in a filtered list which is filtered by the tags or the search. On this first screen we have little previews of the notes as list items. The list items have a title that I give to each of the notes.  It also has a timestamp and it has the tags. That is everything that I need for this list items in the notes page. The title of the note comes from the document itself. The title is always the first line of the note document.  The note documents are basically markdown.  So there will be a hash in the first line which the title in the preview of course should ignore. The first item in the list should always be a new note. So it literally says "+ New Note". And when I click on this or enter in this, a new note will be created in the editor. This is a summary of the first screen. Let's move on to the next screen which opens when I click onto a note. This is the editor view of the note in which I write plain markdown.  If possible, this should have a nice markdown preview.  So this screen is basically a markdown editor that I can leave by pressing ESC which brings me back to the list overview. In the editor I can see the timestamp in which I created the document and I also have the ability to edit the text of the document.  The text should have a suggestion when I type so that I can select tags that I have already set up in other documents.  A key aspect of this app is that I want to have to-dos extracted from my notes. For me, to-dos are basically markdown to-dos, which means I note them by „[ ]“ or „- [ ]“. When I leave a document I want the app to create lines that I started with „o „ to convert them into a markdown „[ ]“ to-do. The app should automatically extract to-dos from these files. This summarizes the editor page. The next page I can enter by pressing T on my keyboard when I am in the overview.  From there I enter a to-do list screen that we can call to-do list. In this screen I will have all the open to-dos from my notes. Firstly, structured and ordered by tags and then secondly by recency.  When I click on a to-do, then I want it automatically to be checked.  The app should go to the original document and mark the to-do check there as well.  The to-do should not disappear from this screen though until I have left the screen myself. When I come back the next time, the lastly checked to-dos will not be visible anymore.  I also want to have the ability to go to the document from which the to-do originates in the to-do list.  The next page is a link to the description.  The next page is a link to the description.  The next page is a link to the description.  The next page is a link to the description.  The next page is a link to the description.  The next page is a link to the description.  The next page is a link to the description.  The next page is a link to the description.  The next page is a link to the description. Another key aspect of the app is that I can switch workspaces.  A workspace basically means I have access to different files, which means different markdown files that are in one folder.  In this folder, we create a new note as a markdown file and its title, which means the filename is only the UNIX timestamp at which it was created.  This folder has a metadata subfolder called „.ponder“ in which we can configure some things.  Also, the folder should have a subfolder called „deleted“ in which the app should move every file that is deleted in the app, which means it's not shown in the app anymore. When I click on delete, the file is not truly gone but moved into the deleted folder. A file is deleted in the app by clicking on it in the overview or having the focus on it, then pressing „d“.  Then the system should ask me if I really want to delete it and then I have to press „d“ again. The „.ponder“ folder is for things like a database if you need to create any for the search. In the app I want to change workspaces by pressing the number keys in the overview: 1 to 9. This means you have to create a metadata file for example as a JSON somewhere in the system on which the app can reference where the workspaces are stored and also which workspace was opened last so that you can reopen it next time So if you open a workspace then you should of course see all the files in the overview but if the workspace is not connected to any folder yet you should have a screen that will prompt you to choose a folder with a dialog. I want the app to be quite modular and I want all the styles to be extracted so that I can modify them easily myself. Maybe the easiest way could be just CSS or something equivalent.  Later I want to add breakpoints for different screen sizes, but I'm not sure about this yet.  Also, I want to have an approach that changes the margins of the setter column in which all the content is by the width of the window as well as the font size, which should be affected by the width of the window so that to some degree the content scales itself to the window, but not to a point where it's not legible anymore. I will provide Figma layouts and components for you to build. Please try to use the Figma design to make the components as close to this as possible.  If something is missing, then you might ask me as well first.  The Figma is provided via the local MCP. 